# Agent anatomy — moved

The canonical Cursor agent registry is **portable across all projects**:

**`~/.cursor/docs/agents_cursor.md`**

Open that file for full agent scopes, lanes, anti-duplication rules, and routing.

This project also has a short pointer at [`.cursor/agents_cursor.md`](../.cursor/agents_cursor.md).

> `@system-builder` maintains the canonical file automatically when agents or skills change.
