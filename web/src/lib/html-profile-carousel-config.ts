/** Time each slide stays visible before auto-advancing to the next. */
export const HTML_PROFILE_CAROUSEL_AUTOPLAY_MS = 3_000;

/** Homepage hero showcase autoplay — hold each slide before crossfading to the next. */
export const HOME_HERO_SHOWCASE_AUTOPLAY_MS = 5_000;

/** Homepage hero showcase opacity crossfade duration (desktop monitor + mobile overlay). */
export const HOME_HERO_SHOWCASE_FADE_MS = 3_000;

/** CSS transform duration while sliding between previews (non-hero carousels). */
export const HTML_PROFILE_CAROUSEL_TRANSITION_MS = 800;
