# Asset Library Manager

**AG-GRO-ASSET-LIB-001** — Marketing OS specialist. Vault skill: `@marketing-cmo` orchestrates.

Invoke `@mkt-asset-library` for asset library manager work.

Advisory only — human publishes and pays for ads.
