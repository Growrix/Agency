# MC-PLT-CODEBASE-001

Ported from `Junks/MCP/mcp-system/dist/.../codebase-intelligence-mcp` (lib/).

Run: `npm install` then configure in `.cursor/mcp.json`.

Snapshots stored under `.cursor/brain/codebase-snapshots/`.
