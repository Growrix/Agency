# MC-KNW-RETRIEVE-001

AI-BOS native retrieval MCP — founder memory + knowledge registry grounding.

Inspired by Junks `knowledge-mcp` retrieve/shape-context (not a direct copy of brain server).

Run: `npm install` then configure in `.cursor/mcp.json`.
