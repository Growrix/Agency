# Twilio — Planned Playbook (P2)

**Status:** Not active in Growrixos v1 runtime config.

Report `missing_knowledge` until env vars are added to `runtime.ts` and product scope confirms SMS/voice.

When activated, document:

- `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER`
- Webhook signature verification for inbound SMS
- Idempotent message handling
