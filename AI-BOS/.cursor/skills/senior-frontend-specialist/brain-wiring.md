# Brain Wiring — Senior Frontend Specialist

## Read order (session start)

1. `.cursor/brain/lane-router.yaml`
2. Lane brain:
   - `web/` → `memories/repo/site-brain.md` + `DOC/PROJECT PLAN/ai-context.yaml`
   - `sites/` → `.cursor/brain/template-brain.md`
   - `Frontend_Nextjs/` → `.cursor/brain/migration-brain.md`
3. Ledger:
   - `web/` → `DOC/PROJECT PLAN/Tasks/tasks.md`
   - templates/migrations → `.cursor/execution/template-tasks.md`
4. Lane skill entrypoint
5. Target files in scope only

## Max context discipline

- Default load cap: **7 files** at session start
- Load integration docs only in P4
- Load conversion maps only when migrating

## Session log

Append to active ledger `## Log`:

```markdown
- <ISO timestamp> | senior-frontend-specialist | P<n> | <summary>
  - evidence: <path>
  - commit: <hash or none>
```

## Resume

Use `/resume-brain` command or read ledger last `in_progress` task.
